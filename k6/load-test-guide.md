# Mock 서버 부하 테스트 실행 가이드

## 배경

알림 발송 Mock 서버의 동시 처리 한계를 찾는 부하 테스트입니다.
이 서버는 요청마다 지수 분포 기반 랜덤 지연(평균 ~4초, 최대 30초)을 적용하고, 1/3 확률로 500 에러를 반환합니다.

## 목적

**동시 요청(VU)을 단계별로 올려가며 Mock 서버가 버티는 한계점을 찾는다.**

### 한계점 판별 기준
- P95 응답시간이 **15초를 초과**하기 시작 → 요청이 Tomcat 쓰레드 대기열에서 줄 서고 있음
- P99 응답시간이 **25초를 초과** → 서버가 심각하게 느려짐
- 에러율(`http_req_failed`)이 **5%를 초과** → 서버가 요청을 거부하기 시작

> 부하 없을 때 이론값 (지수분포 평균 4초): P50≈2.8s, P95≈12s, P99≈18s
> threshold는 이보다 넉넉하게 잡아서, 초과하면 "서버 과부하"로 판단

## 사전 조건

- Mock 서버가 윈도우(또는 별도 머신)에서 실행 중이어야 함
- k6가 설치되어 있어야 함 (`brew install k6`)
- 부하 테스트 스크립트: `k6/breakpoint-test.js`

## 테스트 시나리오

### Breakpoint Test (breakpoint-test.js)

VU를 단계적으로 올리며 임계치를 탐색합니다.

**Test2 시나리오 (현재 활성):**
| 단계 | VU | 유지 시간 |
|------|-----|----------|
| 1 | 50 | 1분 |
| 2 | 100 | 1분 |
| 3 | 200 | 1분 |
| 4 | 300 | 1분 |
| 5 | 400 | 1분 |
| 쿨다운 | 0 | 30초 |

## 실행 방법

### 기본 실행
```bash
k6 run k6/breakpoint-test.js
```

### Grafana에서 VU 함께 보기 (Prometheus Remote Write)
```bash
K6_PROMETHEUS_RW_SERVER_URL=http://localhost:9090/api/v1/write \
  k6 run --out experimental-prometheus-rw k6/breakpoint-test.js
```
> Prometheus 실행 시 `--web.enable-remote-write-receiver` 플래그 필요

## 결과 기록 형식

| Round | VU | avg 응답시간 | P95 응답시간 | 에러율 | TPS | 판정 |
|-------|-----|------------|------------|--------|-----|------|
| 1 | 50  | ?s | ?s | ?% | ? | ✅ or ❌ |
| 2 | 100 | ?s | ?s | ?% | ? | ✅ or ❌ |
| 3 | 200 | ?s | ?s | ?% | ? | ✅ or ❌ |
| 4 | 300 | ?s | ?s | ?% | ? | ✅ or ❌ |
| 5 | 400 | ?s | ?s | ?% | ? | ✅ or ❌ |

### 판정 기준
- ✅ = P95 응답시간 15초 이하, 에러율 5% 이하
- ❌ = P95 응답시간 15초 초과 OR 에러율 5% 초과

### 최종 결론
처음으로 ❌가 나온 Round의 직전 VU 수 = **Mock 서버 동시 처리 한계**
